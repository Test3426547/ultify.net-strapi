'use strict';

/**
 * contact-form-full controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::contact-form-full.contact-form-full');
